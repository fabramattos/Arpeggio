logging:
  level:
    root: DEBUG
    br.com.arpeggio.api: DEBUG
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level %logger{36} - %msg%n"

server:
  port: ${PORT}
  address: 0.0.0.0
#  forward-headers-strategy: framework
  tomcat:
    connection-timeout: 30s # Exemplo: 30 segundos
    socket-timeout: 30s # Tempo limite para operações de socket